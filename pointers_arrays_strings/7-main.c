#include "main.h"

/**
 * main - check the code for puts_half function
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char str1[] = "0123456789";
	char str2[] = "Holberton";
	char str3[] = "ALX School";

	puts_half(str1);
	puts_half(str2);
	puts_half(str3);

	return (0);
}
